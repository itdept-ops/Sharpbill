# Frontend build-time vars (public identifiers, safe to bake into the bundle).
# For docker-compose these are supplied from the root .env via GOOGLE_CLIENT_ID / AZURE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
VITE_AZURE_CLIENT_ID=
